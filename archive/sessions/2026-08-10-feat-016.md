# Session 2026-08-10 · feat-016 (kevin-malik)

Started from a clean baseline (`HARNESS_VERIFY: PASS (build)`), picked feat-016 — the one feature
whose dependencies (feat-012, feat-015) were both ✅. Closed it in one session.

## Changes

| File | What | Why |
|---|---|---|
| `desktop/src-tauri/src/test_session.rs` | new — `TestSession`, `start()`, `stop()`, 3 commands, 5 tests | FR-012 / FR-014; the run's scope is snapshotted so a later plan edit can't rewrite history |
| `desktop/src-tauri/src/lib.rs` | `pub mod test_session` + the three commands | the webview reaches the store through the invoke handler |
| `desktop/src-tauri/src/test_case.rs` | `actor()` → `pub(crate)` | a session's `started_by` must come from the same cached account as every other audit field, never from the webview |
| `desktop/src-tauri/src/test_plan.rs` | `load()` → `pub(crate)` | starting from a plan has to resolve it from the store |
| `desktop/src/Runner.tsx` | new — start form, running / stopped lists, stop-with-result prompt | FR-012 / FR-014 |
| `desktop/src/App.tsx` | shell owns the session list; FR-056d count from the store; FR-053a refusal moved into Runner | the guard applies on every screen and survives a restart; blanking the Runner would hide the running sessions FR-053a says are unaffected |
| `desktop/src/__tests__/Runner.test.tsx` | new — 7 tests | start payload, ad hoc scope, refusal, stop prompt, cancel, listing, expired grace |
| `desktop/src/__tests__/App.test.tsx` | FR-056d driven by `list_test_sessions`, not a prop | the prop no longer exists — a shell that never opened the Runner must still refuse the switch |
| `FEATURES.md` | feat-016 🟡 → 🔵 → ✅, epic 13/21 → 14/21 | status in real time |
| `archive/features/feat-016.md` | new | evidence rotated out of the hot files |

## Verify

`./verify.sh build` → `HARNESS_VERIFY: PASS (build)`
`./verify.sh test` → `HARNESS_VERIFY: PASS (test)` — Vitest 50/50, Rust 67/67.

## Notes for whoever picks up next

- `test_session.rs` and `ws/server.rs` now share one key: the SDK-reported `device_id`. feat-017's
  log viewer can go straight from a session row to `Sessions::records(device_id, session_id)` —
  what it still lacks is the WS **session id** on the `TestSession` row. The device supplies it at
  handshake; the desktop's session is minted before the device is told to start. Deciding which
  side names the run is feat-017's first question, not a gap in this one.
- Nothing is committed. `git status` shows the full change set.
