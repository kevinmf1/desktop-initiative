# Session · 2026-08-10 · kevin-malik · feat-015

Baseline `HARNESS_VERIFY: PASS (build)` before any edit. Closed feat-015; nothing else taken.

## Changes

| File | What | Why |
|---|---|---|
| `desktop/src-tauri/Cargo.toml` | added `tokio-tungstenite` (`handshake` only, no TLS) and `futures-util` (`sink`) | nothing in the graph speaks RFC 6455 — see the 2026-08-10 decision in `CONSTITUTION.md` |
| `desktop/src-tauri/src/ws/server.rs` | new — gate (`admit`), connection (`handle`), session store (`Sessions`), listener (`start`), two commands | FR-021: the `device-desktop-ws` server itself |
| `desktop/src-tauri/src/ws/mod.rs` | `pub mod server`; `hello` gained `platform` / `os_version` / `display_name`; `paired` gained `device` / `reconnect_credential` | the contract's frames carry them; FR-022 needs the platform from the first handshake |
| `desktop/src-tauri/src/ws/pairing.rs` | `PairingNack::refused(reason, message)` | the contract has five nack reasons, not one; `expired_token` now delegates |
| `desktop/src-tauri/src/device.rs` | `save` is `pub(crate)`; corrected the `Observation` doc | the gate has to persist a registration it just made; the old comment claimed `hello` carries no platform |
| `desktop/src-tauri/src/lib.rs` | manage `WsServer`, `.setup()` starts the listener, two commands registered | the hot path must be up before any screen opens (Principle III) |
| `desktop/src/Devices.tsx` | `Device sessions` panel, polled on the existing 1s countdown tick | FR-021's "visible" half |
| `desktop/src/App.tsx` | `set_active_workspace` on mount and on switch | the Rust gate cannot ask React which workspace is open |
| `desktop/src/__tests__/Devices.test.tsx` | session fixtures + concurrency, empty-state and label tests | evidence for the visible half |
| `desktop/src/__tests__/App.test.tsx` | allow `set_active_workspace` in the two call-list assertions | it is local scoping, not a write — the no-reattribution assertion is kept and tightened |
| `CONSTITUTION.md` | dated decision: `tokio-tungstenite`, and hot-path code testable without the app | an arguable dependency choice, settled with its reasoning |

## Verify

`./verify.sh build` → `HARNESS_VERIFY: PASS (build)`
`./verify.sh test` → `HARNESS_VERIFY: PASS (test)` · Vitest 43/43 · Rust 62/62

## Note for the next session

Nothing is proven against a real SDK — none exists in this repo. See *Not proven here* in
[feat-015.md](../features/feat-015.md).
