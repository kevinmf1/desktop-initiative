# Session 2026-08-06 — feat-024 (debug-only local authentication)

**By:** kevin-malik · **Baseline:** `HARNESS_VERIFY: PASS (build)` and
`HARNESS_VERIFY: PASS (test)` before edits.

## What happened

Added a Rust-owned local account behind `TESTLAB_DEV_AUTH=1` so authenticated standalone desktop
features can be exercised from a running Tauri debug build without Google or a backend. The fixture
has two workspaces to cover switching and isolation, while the release implementation always
returns no development account. Full detail and evidence: [../features/feat-024.md](../features/feat-024.md).

## Changes

| File | Change | Why |
|------|--------|-----|
| `FEATURES.md` · `state/kevin-malik.md` | feat-024 tracked as the sole active feature, then closed | Preserve one-feature-at-a-time attribution and resumability |
| `desktop/src-tauri/src/auth_session.rs` | debug-only local account, two active workspaces, sign-out guard and unit test | Exercise the real authenticated Rust/UI path with no Google/backend dependency |
| `CONSTITUTION.md` | recorded the debug-only auth exception and its evidence boundary | Keep production's Google-only rule explicit |
| `specs/frontend/quickstart.md` | launch commands for Unix shells and PowerShell; scope limitations | Make manual testing reproducible |

## Verification

- `cargo check --release --all-targets --manifest-path desktop/src-tauri/Cargo.toml` ✅
- `HARNESS_VERIFY: PASS (build)`
- `HARNESS_VERIFY: PASS (test)` — Vitest 41/41, Rust 55/55

An initial release all-target check found that the unit-test helpers were hidden by
`cfg(debug_assertions)`. They moved to `cfg(any(debug_assertions, test))`; the runtime function
remains split so release builds still return `None`. The repeated release check then passed.

## Handoff

Run `TESTLAB_DEV_AUTH=1 npm run tauri dev` from `desktop/` for manual standalone checks. Resume
feat-015 next: bind the `device-desktop-ws` listener and wire reconnect → pairing authorization →
registry admission before records reach viewers or storage.
