# Session — 2026-08-06 · feat-012

- **By:** kevin-malik
- **Outcome:** feat-012 closed ✅
- **Verification:** `HARNESS_VERIFY: PASS (build)` · `HARNESS_VERIFY: PASS (test)`; Vitest
  31/31, Rust 40/40

## Changes

| File | Change | Why |
|------|--------|-----|
| `desktop/src-tauri/src/test_plan.rs` | Test Plan/Item store, validation, four IPC commands, six tests | FR-009…011 business rules and workspace boundary |
| `desktop/src-tauri/src/lib.rs` · `test_case.rs` | register commands; expose case load inside the crate | Plan writes validate canonical case ids in Rust |
| `desktop/src/TestPlans.tsx` · `App.tsx` | real plan CRUD/membership screen and navigation wiring | Replace feat-012 placeholder |
| `desktop/src/TestCases.tsx` | derive real instances from stored plan items | Feed FR-003a summary status from actual membership |
| `desktop/src/__tests__/TestPlans.test.tsx` · `TestCases.test.tsx` · `App.test.tsx` | 4 new tests and command/read expectation updates | UI/IPC proof and workspace re-read coverage |
| `FEATURES.md` · `archive/features/feat-012.md` · `state/kevin-malik.md` | feature lifecycle, evidence, and handoff | Harness definition of done |

## What went wrong

- One UI assertion searched for `Lifecycle: Archived` as one text node, while the label and value are
  intentionally split across `<strong>` and its parent. The UI was correct; the assertion now checks
  the parent's full text.
- The first `cargo fmt` invocation ran from the repository root, which has no `Cargo.toml`. Re-run with
  `--manifest-path desktop/src-tauri/Cargo.toml`; no product change was involved.
- Review caught an initial validator that would reject editing a plan after one of its linked cases was
  soft-deleted. It now permits retaining an existing historical link while still refusing new deleted
  or cross-workspace links, with a regression test.
